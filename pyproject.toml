[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_functions = ["test_*"]
addopts = "-v --tb=short --ignore=tests/e2e"
markers = [
    "e2e: end-to-end browser tests requiring live frontend + backend (deselect with --ignore=tests/e2e)",
]
